function Calculate(numberOfSteps, lenghtOfAstepInMeters, studentSpeedKMh) {

    let metersPerMInutePassed = studentSpeedKMh / 60;
    const goalCrossedMettersBeforeBreak = 500;
    const breakInMinutes = 1;

    let stepsCrossedBeforeTheBreak = goalCrossedMettersBeforeBreak * metersPerSecondPassed;
    let numberOfBreaks = Math.floor(numberOfSteps / metersCrossedBeforeTheBreak);

    let timeWalkedBefforeABreak = metersCrossedBeforeTheBreak * metersPerSecondPassed;
    let totalTimeWalked = (numberOfBreaks * breakInMinutes) + (numberOfSteps / metersCrossedBeforeTheBreak)

    let result = `${}`
}